# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Quant (K线图系统) 是一个完整的股票K线图查询系统，支持5400只A股的日K线数据查询和可视化。

## Architecture

```
前端 (React + ECharts)  ←→  后端 (FastAPI)  ←→  ClickHouse (通过SSH隧道)
     :5173                    :8001                192.168.50.90:8123
                                ↓
                           Redis 缓存
```

### 数据流

```
用户请求 → FastAPI → Redis缓存检查 → ClickHouse查询 → 返回K线数据 → ECharts渲染
```

## Tech Stack

### 后端
- FastAPI 0.109.0
- ClickHouse Connect 0.10.0
- Redis 5.0.1
- Pandas 2.3.3

### 前端
- React 18 + TypeScript
- Vite 7.3.1
- ECharts (K线图渲染)
- Ant Design (UI组件)
- Zustand (状态管理)
- Axios (HTTP客户端)

## Project Structure

```
Quant/
├── kline-backend/          # 后端 FastAPI 项目
│   ├── app/
│   │   ├── api/           # API 路由
│   │   ├── core/          # 核心配置 (SSH隧道)
│   │   ├── db/            # 数据库客户端 (ClickHouse, Redis)
│   │   ├── schemas/       # Pydantic 模型
│   │   ├── services/      # 业务逻辑
│   │   └── main.py        # FastAPI 入口
│   └── requirements.txt
│
└── kline-frontend/        # 前端 React 项目
    ├── src/
    │   ├── api/          # API 客户端
    │   ├── components/   # React 组件
    │   ├── pages/        # 页面
    │   ├── store/        # Zustand 状态管理
    │   └── types/        # TypeScript 类型
    └── package.json
```

## Common Commands

### 开发环境启动

#### 推荐方式：使用调试脚本 🚀

最简单快速的启动方式，支持一键启动和停止：

```bash
# 启动所有服务 (SSH隧道 + 后端 + 前端)
./start-debug.sh

# 或分别启动各组件
./start-debug.sh tunnel     # 仅启动SSH隧道
./start-debug.sh backend    # 仅启动后端
./start-debug.sh frontend   # 仅启动前端

# 停止所有服务
./stop-debug.sh

# 或分别停止各组件
./stop-debug.sh tunnel
./stop-debug.sh backend
./stop-debug.sh frontend
```

**调试脚本说明：**
- `start-debug.sh` 和 `stop-debug.sh` 自动化启动/停止所有服务
- 后端和前端需要独立终端运行（脚本会给出详细指导）
- 使用 `tmux` 可同时在后台运行多个服务
- 所有服务都在前台运行，便于查看日志和调试

#### 手动启动（不使用脚本）

```bash
# 1. 建立 SSH 隧道 (连接远程 ClickHouse)
ssh -N -L 18123:localhost:8123 wsl &

# 2. 启动后端
cd kline-backend
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload

# 3. 启动前端 (新终端)
cd kline-frontend
npm run dev
```

#### 使用 tmux 同时运行多个服务

```bash
# 创建会话并启动后端
tmux new-session -d -s quant -c "$(pwd)" './start-debug.sh backend'

# 创建新窗口启动前端
tmux new-window -t quant -c "$(pwd)" './start-debug.sh frontend'

# 查看日志
tmux attach -t quant

# 停止所有服务
./stop-debug.sh all
```

### 生产环境部署

```bash
# 后端 (后台运行)
cd kline-backend
source venv/bin/activate
nohup uvicorn app.main:app --host 0.0.0.0 --port 8001 > server.log 2>&1 &

# 前端打包部署
cd kline-frontend
npm run build
# 将 dist/ 目录内容复制到网关静态目录
cp -r dist/* ~/workspace/project/gateway/data/www/kline/
```

### 服务状态检查

```bash
# 检查后端
curl http://localhost:8001/health

# 检查 SSH 隧道
ps aux | grep "ssh -N -L 18123"

# 检查 ClickHouse 连接
curl "http://localhost:8001/api/stocks/list?keyword=平安&limit=5"
```

### 重启服务

```bash
# 找到并停止后端进程
ps aux | grep uvicorn | grep -v grep
kill <PID>

# 重新启动
cd kline-backend
source venv/bin/activate
nohup uvicorn app.main:app --host 0.0.0.0 --port 8001 > server.log 2>&1 &
```

## 调试脚本参考

本项目提供了便捷的调试脚本，方便开发者和 AI 助手快速启动/停止服务：

### 脚本位置

- `start-debug.sh` - 启动服务脚本
- `stop-debug.sh` - 停止服务脚本

### 脚本功能说明

#### start-debug.sh

启动本地调试服务，支持按需启动指定组件。

**使用方式：**
```bash
./start-debug.sh [component]
```

**参数说明：**

| 参数 | 说明 | 何时使用 |
|------|------|---------|
| `all`（默认） | 启动SSH隧道，给出启动后端和前端的指导 | 首次启动或重新配置 |
| `tunnel` | 仅启动SSH隧道到远程ClickHouse | 仅需数据库连接 |
| `backend` | 启动FastAPI后端（需要终端1） | 开发后端API |
| `frontend` | 启动React前端开发服务器（需要终端2） | 开发前端界面 |

**示例：**
```bash
# 启动SSH隧道
./start-debug.sh tunnel

# 在新终端1启动后端
./start-debug.sh backend
# 输出: INFO: Uvicorn running on http://127.0.0.1:8001

# 在新终端2启动前端
./start-debug.sh frontend
# 输出: VITE v7.3.1 ready in xxx ms
#       Local: http://localhost:5173/
```

#### stop-debug.sh

停止本地调试服务。

**使用方式：**
```bash
./stop-debug.sh [component]
```

**参数说明：**

| 参数 | 说明 |
|------|------|
| `all`（默认） | 停止所有服务 |
| `tunnel` | 仅停止SSH隧道 |
| `backend` | 仅停止后端 |
| `frontend` | 仅停止前端 |

**示例：**
```bash
# 停止所有服务
./stop-debug.sh

# 仅停止后端（继续测试前端）
./stop-debug.sh backend
```

### AI 助手使用指南

当 AI 助手帮助开发或调试时，可以使用以下命令：

```bash
# 在进行开发/调试前，确保所有服务都已启动
./start-debug.sh tunnel      # 启动数据库隧道
./start-debug.sh backend      # 新终端: 启动后端
./start-debug.sh frontend     # 新终端: 启动前端

# 开发完成后，清理服务
./stop-debug.sh all          # 停止所有服务
```

### 常见场景

**场景1：仅开发后端API**
```bash
./start-debug.sh tunnel      # 终端1: 隧道
./start-debug.sh backend     # 终端2: 后端
# 使用 http://localhost:8001/docs 查看API文档
```

**场景2：仅开发前端界面**
```bash
./start-debug.sh tunnel      # 终端1: 隧道
./start-debug.sh backend     # 终端2: 后端（Mock API）
./start-debug.sh frontend    # 终端3: 前端
```

**场景3：快速重启服务**
```bash
./stop-debug.sh backend      # 停止后端
./start-debug.sh backend     # 重启后端
```

## API Endpoints

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/stocks/list` | GET | 股票列表搜索 |
| `/api/kline/data` | GET | 获取K线数据 |
| `/health` | GET | 健康检查 |

### API 示例

```bash
# 搜索股票
curl "http://localhost:8001/api/stocks/list?keyword=平安&limit=5"

# 获取K线数据
curl "http://localhost:8001/api/kline/data?code=000001.SZ&start_date=2024-01-01&end_date=2024-12-31&adj_type=after"
```

## Gateway Configuration

通过 Nginx 网关访问：`http://192.168.50.90/kline/`

- 静态文件: `/kline/` → `~/gateway/data/www/kline/`
- API 代理: `/kline/api/` → `http://127.0.0.1:8001/api/`
- 健康检查: `/kline/health` → `http://127.0.0.1:8001/health`

## Data Notes

- **数据源**: ClickHouse 数据库 (`stock.minute_kline` 表)
- **股票数量**: 5400只 A股
- **复权方式**: 支持后复权/前复权/不复权
- **K线类型**: 当前支持日K线

## Completed Features

### 基础功能
- ✅ 股票搜索 (代码和名称模糊搜索)
- ✅ 股票数据时间范围查询
- ✅ Redis 缓存 (智能TTL策略)
- ✅ SSH 隧道自动管理

### K线图功能
- ✅ 日K线图显示 (ECharts candlestick)
- ✅ 周K/月K/年K线支持
- ✅ 分钟级K线 (1/5/15/30/60分钟)
- ✅ 成交量柱状图
- ✅ 时间范围拖动选择 (dataZoom)
- ✅ 复权类型切换 (不复权/后复权/前复权)

### 技术指标
- ✅ MA 均线 (支持多周期)
- ✅ MACD (快慢线和柱状图)
- ✅ KDJ (随机指标)
- ✅ RSI (相对强弱指标)
- ✅ BOLL 布林带
- ✅ 指标动态叠加和子图显示

### 多股对比
- ✅ 最多5只股票同时对比
- ✅ 涨跌幅归一化对比
- ✅ 支持多周期对比

### 量化回测
- ✅ 5种内置策略 (MA/MACD/KDJ/RSI/BOLL)
- ✅ 策略参数动态配置
- ✅ 完整的回测引擎 (买卖、手续费、印花税)
- ✅ 绩效指标计算 (收益率、夏普比率、最大回撤等)
- ✅ 买入持有基准对比
- ✅ 资金曲线可视化
- ✅ 交易记录明细
- ✅ 自动设置股票全周期回测

## Recent Updates (2026-02-02)

### 新功能实现

#### 1. 量化策略回测系统 ✨
**后端实现**
- 创建回测引擎 (`app/services/backtest/engine.py`)
  - BacktestEngine: 管理资金、持仓、交易执行
  - 支持手续费计算（万三佣金 + 千一印花税）
  - 整百股交易限制
  - 每日账户状态记录

- 策略工厂 (`app/services/backtest/strategies.py`)
  - MA均线交叉策略（金叉买入、死叉卖出）
  - MACD策略（DIF/DEA交叉）
  - KDJ策略（超买超卖反转）
  - RSI策略（超买超卖区间突破）
  - 布林带策略（触及上下轨反转）
  - 策略参数完全可配置

- 绩效指标计算 (`app/services/backtest/metrics.py`)
  - 总收益率、年化收益率
  - 最大回撤、夏普比率
  - 胜率、盈亏比
  - 交易统计（总次数、盈利次数、亏损次数）

- API端点 (`app/api/endpoints/backtest.py`)
  - `GET /api/backtest/strategies` - 获取策略列表及参数定义
  - `POST /api/backtest/run` - 执行回测

**前端实现**
- 回测页面 (`src/pages/BacktestPage/index.tsx`)
  - 左右分栏布局（配置 + 结果）
  - 股票选择、日期范围、策略配置
  - 运行回测、重置功能
  - 结果展示自动滚动

- 策略配置面板 (`src/components/StrategyPanel/index.tsx`)
  - 策略选择（下拉框带说明）
  - 动态参数表单（根据策略定义生成）
  - 初始资金、仓位比例配置

- 回测结果展示 (`src/components/BacktestResult/index.tsx`)
  - 8个核心绩效指标卡片
  - 资金曲线图（ECharts双线对比）
  - 交易记录表格（可分页、筛选）

#### 2. 买入持有基准对比 📊
- 回测时自动计算买入持有基准收益
- 绩效指标新增：
  - 买入持有收益率
  - 超额收益（策略 vs 基准）
- 资金曲线图显示双线对比
  - 策略收益（实线+填充）
  - 买入持有（蓝色虚线）

#### 3. 股票数据时间范围查询 📅
- 新增 API: `GET /api/stocks/date-range?code=xxx`
- StockService 新增 `get_stock_date_range()` 方法
- 回测页面选择股票后自动填充全周期日期

#### 4. 用户体验优化 🎨
- 回测完成后自动滚动到结果区域
- 右侧结果区域独立滚动容器
- 左侧配置面板保持固定可见

### Bug 修复

#### 1. BacktestMetrics 初始化顺序错误
- **问题**: `win_rate` 计算时 `total_trades` 尚未定义
- **修复**: 调整属性初始化顺序，先计算基础数据再计算派生指标

#### 2. 数据库客户端导入错误
- **问题**: `compare.py` 和 `backtest.py` 使用了不存在的 `get_clickhouse_client`
- **修复**: 改为导入 `db_client` 全局单例

### 已知问题

⚠️ **策略信号未执行问题**
- 症状: 生成了交易信号（如101个），但交易次数为0
- 原因: 可能是信号日期与K线数据日期格式不匹配
- 状态: 已添加调试日志，待进一步排查

## Architecture Updates

### API 端点总览

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/stocks/list` | GET | 股票列表搜索 |
| `/api/stocks/date-range` | GET | 获取股票数据时间范围 |
| `/api/kline/data` | GET | 获取K线数据（支持多周期、技术指标） |
| `/api/kline/minute` | GET | 获取分钟级K线 |
| `/api/compare/data` | GET | 多股对比数据 |
| `/api/backtest/strategies` | GET | 获取策略列表 |
| `/api/backtest/run` | POST | 执行回测 |
| `/health` | GET | 健康检查 |

### 前端页面结构

```
App (Tabs)
├── K线图 (KLinePage)
│   ├── StockSelector
│   ├── AdjTypeSelector
│   ├── PeriodSelector
│   ├── IndicatorSelector
│   └── KLineChart (with indicators)
├── 多股对比 (ComparePage)
│   ├── MultiStockSelector
│   └── CompareChart
└── 策略回测 (BacktestPage)
    ├── 配置面板
    │   ├── StockSelector
    │   ├── DateRangePicker
    │   └── StrategyPanel
    └── BacktestResult
        ├── 绩效指标
        ├── 资金曲线
        └── 交易记录
```
